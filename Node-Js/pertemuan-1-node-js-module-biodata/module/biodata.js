function name(name) {
 return `Nama: ${name}`;
}

function place(place) {
 return `Tempat Lahir: ${place}`;
}

function birtOfDate(date) {
 return `Tanggal Lahir: ${date}`;
}

function address(address) {
 return `Alamat: ${address}`;
}

module.exports = { name, place, birtOfDate, address };
